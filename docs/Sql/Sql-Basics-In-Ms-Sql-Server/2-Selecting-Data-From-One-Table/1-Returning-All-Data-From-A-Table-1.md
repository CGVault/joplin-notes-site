
## Get all data from a table

To get all data from a table we use `select * from tablename` . Ending SQL statements in a ; or in GO is best practice. ; ends a single script which GO ends multiple scripts as a batch. Using GO 5 runs all the scripts in the batch 5 times.
<br/>

&nbsp;
