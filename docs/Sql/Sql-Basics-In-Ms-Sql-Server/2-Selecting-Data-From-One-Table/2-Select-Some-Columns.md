
## Selecting one column

Use `select columnname from table;` to only get one column.

## Selecting more than one column

Use `select columnname, columnname2 from table;` to only get more than one column. Each column selected needs a comma but the last one.

&nbsp;
